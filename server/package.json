{
  "name": "server",
  "version": "1.0.0",
 
  "description": "This is the node backend of mern stack blog site",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "client-install": "cd client && npm install",
    "client-build": "cd client && npm run build",
    "heroku-postbuild": "npm run client-install && npm run client-build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": "16.13.0"
  },
  "author": "Nouman Ali",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.1.0",
    "body-parser": "^1.20.1",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "gridfs-stream": "^1.1.1",
    "hbs": "^4.2.0",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^6.7.0",
    "multer": "^1.4.5-lts.1",
    "multer-gridfs-storage": "^5.0.2"
  }
}
